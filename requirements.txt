# SIH26051_Shelter — RUNTIME/API dependencies (this file is what Vercel installs).
# Lean on purpose: the Vercel Python function bundle must stay small
# (bundles > ~250 MB break routing — see docs/deployment.md). Heavy
# scientific libs (pvlib, scipy, h5py) are replaced by the verbatim
# pure-NumPy ports in src/data/solar.py (validated bit-equivalent in
# scripts/validate_solar_math.py). Local dev extras live in requirements-dev.txt.
#
# Install both locally:  python -m pip install -r requirements.txt -r requirements-dev.txt

# --- core data + model ---
numpy==2.3.5          # pinned: validated engine bit-exactness (see src/data/solar.py)
pandas==2.2.3         # pinned: pandas 3.x changed tz-aware asi8 unit + resample defaults
requests>=2.31
PyYAML>=6.0

# --- API / deployment ---
fastapi>=0.110
python-multipart>=0.0.9   # multipart file uploads (POST /api/cad/import)
pydantic>=2.5
uvicorn>=0.27        # local dev server only; Vercel runs ASGI natively
python-dotenv>=1.0

# --- cloud persistence ---
# (no supabase SDK: Store talks to PostgREST directly over requests)

# --- optimization ---
optuna>=3.5
