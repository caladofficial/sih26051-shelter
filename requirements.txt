# SIH26051_Shelter — RUNTIME/API dependencies (this file is what Vercel installs).
# Lean on purpose: the Vercel Python function must stay small & cold-start friendly.
# Local dev extras (dashboard, plots, tests) live in requirements-dev.txt.
#
# Install both locally:  python -m pip install -r requirements.txt -r requirements-dev.txt

# --- core data + model ---
numpy>=1.24
pandas>=2.0
pvlib>=0.10          # solar position + POA irradiance (works without scipy)
requests>=2.31
PyYAML>=6.0

# --- API / deployment ---
fastapi>=0.110
pydantic>=2.5
uvicorn>=0.27        # local dev server only; Vercel runs ASGI natively
python-dotenv>=1.0

# --- cloud persistence ---
supabase>=2.4        # Postgres storage when SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY are set

# --- optimization ---
optuna>=3.5
